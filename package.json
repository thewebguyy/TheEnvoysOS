{
    "name": "theenvoysos",
    "version": "1.0.0",
    "scripts": {
        "setup": "npm install && cd server && npm install && cd ../client && npm install",
        "dev": "node dev.js",
        "start": "node dev.js"
    },
    "dependencies": {
        "bcryptjs": "^3.0.3",
        "dotenv": "^17.2.3",
        "express-rate-limit": "^8.2.1",
        "fs-extra": "^11.3.3",
        "helmet": "^8.1.0",
        "jsonwebtoken": "^9.0.3"
    },
    "engines": {
        "node": ">=20.0.0"
    }
}